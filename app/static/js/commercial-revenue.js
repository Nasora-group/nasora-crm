document.addEventListener("DOMContentLoaded", async function () {
  const match = window.location.pathname.match(/^\/commercial_dashboard\/([^/]+)\/?$/); if (!match) return;
  const username=decodeURIComponent(match[1]), anchor=document.querySelector(".crm-section-title"); if(!anchor)return;
  const card=document.createElement("section"); card.className="summary-card commercial-revenue-card"; card.style.marginTop="18px";
  card.innerHTML='<div class="section-heading"><div><span class="crm-overline">CHIFFRE D’AFFAIRES</span><h2>CA <span class="commercial-revenue-division"></span></h2><p class="crm-subtitle">CA réalisé par ce commercial, uniquement sur sa division NASMEDIC ou NASDERM.</p></div><strong class="commercial-revenue-total">Chargement…</strong></div><div style="height:280px;position:relative;margin-bottom:18px"><canvas aria-label="Évolution mensuelle du chiffre d’affaires"></canvas></div><div class="table-responsive table-scroll"><table class="responsive-table" style="min-width:650px"><thead><tr><th>Mois</th><th>CA réalisé</th><th>Action</th></tr></thead><tbody><tr><td colspan="3">Chargement…</td></tr></tbody></table></div>';
  anchor.parentNode.insertBefore(card,anchor);
  const divisionEl=card.querySelector(".commercial-revenue-division"), totalEl=card.querySelector(".commercial-revenue-total"), tbody=card.querySelector("tbody"), canvas=card.querySelector("canvas");
  const money=v=>new Intl.NumberFormat("fr-FR",{maximumFractionDigits:0}).format(v||0)+" FCFA";
  const monthLabel=v=>{const [y,m]=v.split("-");return new Date(Number(y),Number(m)-1,1).toLocaleDateString("fr-FR",{month:"long",year:"numeric"});};
  try{
    const response=await fetch("/commercial_dashboard/"+encodeURIComponent(username)+"/revenue-data",{headers:{"X-Requested-With":"XMLHttpRequest"},cache:"no-store"}); if(!response.ok)throw new Error();
    const data=await response.json(); divisionEl.textContent=data.division_label||data.division||""; totalEl.textContent="Total : "+money(data.total); tbody.innerHTML="";
    if(!data.months||!data.months.length){tbody.innerHTML='<tr><td colspan="3">Aucun CA enregistré pour ce commercial.</td></tr>';return;}
    data.months.forEach(row=>{const tr=document.createElement("tr");tr.innerHTML=`<td><strong>${monthLabel(row.month)}</strong></td><td><strong>${money(row.revenue)}</strong></td><td><button type="button" class="btn btn-outline commercial-revenue-detail" data-month="${row.month}">Voir le détail</button></td>`;tbody.appendChild(tr);});
    if(typeof Chart!=="undefined")new Chart(canvas,{type:"bar",data:{labels:data.months.map(r=>monthLabel(r.month)),datasets:[{label:"CA",data:data.months.map(r=>r.revenue),borderWidth:1,borderRadius:6}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{beginAtZero:true,ticks:{callback:v=>money(v)}},x:{ticks:{autoSkip:false,maxRotation:45,minRotation:0}}}}});
    tbody.addEventListener("click",async event=>{const button=event.target.closest(".commercial-revenue-detail");if(!button)return;const month=button.dataset.month;tbody.querySelectorAll(".commercial-revenue-detail-row").forEach(r=>r.remove());button.disabled=true;button.textContent="Chargement…";try{const r=await fetch("/commercial_dashboard/"+encodeURIComponent(username)+"/revenue-detail/"+encodeURIComponent(month),{headers:{"X-Requested-With":"XMLHttpRequest"},cache:"no-store"});if(!r.ok)throw new Error();const detail=await r.json(),detailRow=document.createElement("tr");detailRow.className="commercial-revenue-detail-row";const rows=detail.rows||[];const body=rows.length?rows.map(item=>`<tr><td>${item.supplier}</td><td>${item.product}</td><td>${item.quantity}</td><td><strong>${money(item.revenue)}</strong></td></tr>`).join(""): '<tr><td colspan="4">Aucun détail produit pour ce mois.</td></tr>';detailRow.innerHTML=`<td colspan="3"><div class="crm-surface" style="padding:14px"><div class="section-heading"><strong>Détail — ${monthLabel(detail.month)}</strong><strong>${money(detail.total)}</strong></div><div class="table-responsive"><table class="responsive-table"><thead><tr><th>Laboratoire</th><th>Produit</th><th>Qté</th><th>CA</th></tr></thead><tbody>${body}</tbody></table></div></div></td>`;button.closest("tr").after(detailRow);}catch(e){const row=document.createElement("tr");row.className="commercial-revenue-detail-row";row.innerHTML='<td colspan="3"><div class="flash flash-error">Impossible de charger le détail du CA.</div></td>';button.closest("tr").after(row);}finally{button.disabled=false;button.textContent="Voir le détail";}});
  }catch(error){card.innerHTML='<div class="section-heading"><div><span class="crm-overline">CHIFFRE D’AFFAIRES</span><h2>CA commercial</h2><p class="crm-subtitle">Le CA de la division du commercial est momentanément indisponible.</p></div></div>';}
});
